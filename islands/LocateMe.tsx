import Component from "$store/components/footer/LocateMe.tsx";
import type { LocateMeProps } from "$store/components/footer/Footer.tsx";

function Island(props: LocateMeProps) {
  return <Component {...props} />;
}

export default Island;
